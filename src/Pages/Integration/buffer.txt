                   {/* <Form key={field.key}>
                     <SlotEmbedForm />
                     {fields.length > 1 ? ( */}
                  {/*      <MinusCircleOutlined
                         className="dynamic-delete-button"
                         onClick={() => remove(field.name)}
                       />
                     ) : null}
                   </Form> */}